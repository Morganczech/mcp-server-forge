# {{escapeMarkdown project.title}}

<!-- mcp-forge:managed:start -->
{{escapeMarkdown project.description}}

This project was generated by MCP Server Forge. It is currently a safe
knowledge-server placeholder and does not yet contain a functional MCP SDK
implementation or knowledge indexer.

## Purpose

Prepare a reviewed knowledge-server layout for `{{server.name}}` without
inventing tools, dependencies, or source data.

## Install

```bash
pnpm install
```

Dependency resolution is intentionally pending; no floating package versions
were generated.

## Run

After the future build and dependency steps are implemented:

```bash
pnpm start
```

See [SYSTEM_PROMPT.md](SYSTEM_PROMPT.md) for safe usage guidance.
<!-- mcp-forge:managed:end -->

Add user-maintained documentation outside the managed markers.
